# Miss Tea Tea 网站升级方案

> 版本：v1.0 · 2026-08-24
> 目标：把静态初稿 `index.html` 升级为「老板可统一管理」的客户端 + 服务端网站，部署在 Vercel，后续关联 Google 域名 `www.missteatea.com`。

---

## 1. 项目概述

| 项目 | 内容 |
|---|---|
| 网站 | Miss Tea Tea 奶茶店官网（Leuven, Belgium） |
| 当前状态 | 单文件静态页 `index.html`，内容全部硬编码 |
| 升级目标 | 内容（菜单/活动/店铺信息/图片）由老板在后台维护，前台自动更新 |
| 功能范围 | **仅内容管理**（不含在线点单/支付，留作二期） |
| 语言 | **中英双语**：英文为主、中文辅助展示（同一卡片内双语言并列，无切换按钮） |
| 部署 | Vercel（现有 `miss-tea-tea.vercel.app`），后续关联 Google 域名 `www.missteatea.com` |

---

## 2. 需求分析

### 2.1 需要管理的对象（从现有页面推导）

| 管理对象 | 现状 | 管理需求 |
|---|---|---|
| 菜单（饮品） | 4 款写死在 HTML | 增/删/改饮品；维护中英文名、描述、**价格**、图片；上下架（停售）；排序；分类 |
| 活动/公告 | 「KU Leuven 学生买一送一」写死 | 发布/停用活动；可选生效起止时间（到期自动下架）；排序 |
| 店铺信息 | 地址、营业时间、Instagram 写死 | 修改地址、营业时间、Instagram、电话、Hero 标语 |
| 页面文案 | 中英混排 | 双语字段统一维护 |

### 2.2 非功能需求

- **SEO**：前台页面静态生成（SSG），利于 Google 收录；正式域名生效后提交 sitemap。
- **安全**：后台需要登录；数据写入仅限老板账号（数据库行级安全 RLS）。
- **成本**：尽量使用免费额度（Vercel Hobby + Supabase Free 足够）。
- **可维护性**：老板只操作后台界面，不接触代码。

---

## 3. 技术选型

### 3.1 推荐：Next.js + Supabase（部署于 Vercel）

```
浏览器 ──► Next.js（Vercel 托管）
             ├── 前台 /        SSG 静态生成，游客浏览（SEO 好）
             ├── 前台 /menu    完整菜单（按分类分组）
             └── 后台 /admin   老板登录后管理（受保护）
                          │
                          ▼
                  Supabase（云托管，免费额度）
             ├── Postgres 数据库（菜单/活动/店铺设置表）
             ├── Auth 登录（老板账号，邮箱+密码）
             ├── Storage 图片存储（饮品照片，公开读取）
             └── Studio（自带可视化后台，可应急直接改数据）
```

**为什么这么选：**

- Vercel 已被选定，天然托管 Next.js，一条命令部署，自动 HTTPS。
- Supabase 一个服务同时解决数据库 + 登录鉴权 + 图片存储三件事，免费额度（500MB 数据库 / 1GB 存储）对奶茶店绰绰有余。
- 不需要自己维护服务器；前台 SSG 渲染，加载快、利于 SEO。
- 老板账号用 Supabase Auth，一行代码搞定登录保护，不用自己写会话逻辑。

### 3.2 备选方案对比

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **Next.js + Supabase（推荐）** | 一个服务搞定 DB+Auth+存储；免费额度够用 | 数据在 Supabase 而非 Vercel | ✅ 工作量最小 |
| Next.js + Vercel Postgres + NextAuth | 全部在 Vercel 生态 | 鉴权和图片存储要自己拼，代码多 | 备选 |
| 静态站 + 无头 CMS（Sanity 等） | 老板用现成 CMS | 多一套系统要学，无定制后台 | 不推荐 |
| React + Node/Express（Render/Fly） | 完全自主 | 太重，要自己管服务器和 HTTPS | 不推荐 |

---

## 4. 数据库设计（Supabase / Postgres）

### 4.1 表结构

**menu_categories —— 菜单分类**

```sql
create table menu_categories (
  id          uuid primary key default gen_random_uuid(),
  name_en     text not null,                -- 分类英文名，如 Milk Tea
  name_zh     text not null,                -- 分类中文名，如 奶茶
  sort_order  int not null default 0,       -- 显示顺序
  created_at  timestamptz not null default now()
);
```

**menu_items —— 菜单项（饮品）**

```sql
create table menu_items (
  id             uuid primary key default gen_random_uuid(),
  category_id    uuid references menu_categories(id) on delete set null,
  name_en        text not null,             -- 英文名，如 Brown Sugar Boba Milk
  name_zh        text not null,             -- 中文名，如 黑糖珍珠奶茶
  description_en text not null default '',
  description_zh text not null default '',
  price          numeric(6,2),              -- 价格（欧元）
  image_url      text not null default '',  -- Supabase Storage 图片路径
  is_available   boolean not null default true,  -- 是否在售（停售则不展示）
  is_featured    boolean not null default false, -- 首页推荐位
  sort_order     int not null default 0,    -- 分类内排序
  created_at     timestamptz not null default now(),
  updated_at     timestamptz not null default now()
);
```

**announcements —— 公告/活动**

```sql
create table announcements (
  id          uuid primary key default gen_random_uuid(),
  title_en    text not null,
  title_zh    text not null,
  body_en     text not null default '',
  body_zh     text not null default '',
  is_active   boolean not null default true,
  starts_at   timestamptz,                  -- 可选：生效开始时间
  ends_at     timestamptz,                  -- 可选：生效结束时间（过期自动隐藏）
  sort_order  int not null default 0,
  created_at  timestamptz not null default now()
);
```

**store_settings —— 店铺设置（key-value）**

```sql
create table store_settings (
  key        text primary key,              -- 如 address / opening_hours / instagram / phone / hero_slogan
  value_en   text not null default '',
  value_zh   text not null default '',
  updated_at timestamptz not null default now()
);
```

### 4.2 行级安全（RLS）

| 表 | 游客（anon） | 老板（authenticated） |
|---|---|---|
| 全部表 | 只读 SELECT | 读写（INSERT/UPDATE/DELETE） |

- 公开读取让前台无需登录即可渲染。
- 写入仅限老板账号，从数据库层杜绝越权。
- 老板账号通过 Supabase Auth 创建（一个邮箱账号即可，可后续加号）。

### 4.3 图片存储

- Supabase Storage 建一个公开 bucket `images`，存放饮品照片。
- 前台用 Next.js `next/image` 优化加载（自动压缩、懒加载）。

---

## 5. 页面与路由结构（Next.js App Router）

### 5.1 前台（公开，SSG 静态生成）

| 路由 | 内容 | 数据来源 |
|---|---|---|
| `/` | Hero（标语+学生福利公告）、推荐饮品、活动、店铺信息、页脚 | announcements / menu_items(featured) / store_settings |
| `/menu` | 完整菜单，按分类分组展示（中英文名、价格、图片、描述） | menu_categories / menu_items |

### 5.2 后台（登录保护）

| 路由 | 功能 |
|---|---|
| `/admin/login` | 老板登录（邮箱+密码） |
| `/admin` | 仪表盘：入口 + 概览 |
| `/admin/menu` | 菜单管理：增删改饮品、上下架、排序、上传图片、管理分类 |
| `/admin/announcements` | 活动管理：新建/编辑/停用活动，设置生效时间 |
| `/admin/settings` | 店铺信息：地址、营业时间、Instagram、电话、Hero 标语 |

### 5.3 接口层

- 前台页面直接读 Supabase（服务端组件），无需自定义 API。
- 后台写操作用 Next.js Route Handlers（如 `PATCH /api/menu/[id]`）配合 Supabase Server Client，校验老板登录态。

---

## 6. 中英双语方案

- 数据层：所有内容字段采用双字段（`name_en` + `name_zh`，`title_en` + `title_zh` …），后台两个输入框并列。
- 展示层：同一卡片内英文为主、中文辅助（沿用现有页面 `Brown Sugar Boba Milk / 黑糖珍珠奶茶` 的样式）。
- **不做**语言切换按钮（用户已确认：中英并列展示即可），省掉一套 i18n 框架和路由改造。

---

## 7. 实施阶段与里程碑

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| **M0 准备** | 注册 Supabase 项目；Vercel 上创建项目；**现在就关联 Google 域名**（DNS 生效需要时间，越早越好） | 域名解析成功；两个域名都能访问 |
| **M1 地基** | 初始化 Next.js + Tailwind 项目；用现有 index.html 的视觉风格还原前台双页面；加入 SEO meta、favicon；部署上线 | `miss-tea-tea.vercel.app` 与 `www.missteatea.com` 均显示新前台 |
| **M2 数据化** | 按第 4 节建表 + 建 RLS；写迁移脚本把现有 4 款饮品、地址、营业时间、学生福利公告迁入数据库；前台改为动态渲染 | 删掉硬编码后页面内容不变 |
| **M3 后台** | Supabase Auth 建老板账号；实现 `/admin` 三个管理模块（菜单/活动/店铺信息）；图片上传 | 老板登录后增删改内容，前台即时生效 |
| **M4 收尾** | 图片上传体验打磨；sitemap.xml + 提交 Google Search Console；页面性能/移动端检查 | Lighthouse 达标；Google 能收录 |

> 二期（不在本次范围）：在线点单、订单管理、Stripe 支付。

---

## 8. 域名与部署配置

### 8.1 现状

- `miss-tea-tea.vercel.app` 已注册，Vercel 自动签发 HTTPS，无需操作。

### 8.2 关联 Google 域名（建议 M0 就做）

1. Vercel 项目 → Settings → Domains → 添加 `www.missteatea.com`（以及根域名 `missteatea.com`）。
2. 在 Google Domains 的 DNS 设置中添加记录：
   - `www` → **CNAME** → `cname.vercel-dns.com`
   - `@`（根域）→ **A** → `76.76.21.21`
3. 等待 DNS 生效（通常几分钟到几小时），Vercel 自动签发并续期 HTTPS 证书。
4. 域名生效后，在项目设置里把主域名切到 `www.missteatea.com`，便于 SEO 统一。

### 8.3 环境变量（Vercel 项目设置中配置）

```
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...   # 仅服务端使用，勿暴露
```

---

## 9. 成本估算

| 项目 | 费用 |
|---|---|
| Vercel Hobby 托管 | 免费 |
| Supabase Free（500MB DB / 1GB 存储 / 5 万月活） | 免费 |
| Google 域名 `missteatea.com` | 约 $12–15/年 |
| **合计** | **仅域名年费，约 $12–15/年** |

注意：Supabase 免费项目若 7 天无 API 调用会被暂停（可一键恢复）；如未来接入点单业务再考虑升级 Pro（$25/月）。

---

## 10. 风险与注意事项

- **数据安全**：Supabase 免费版无自动备份，建议每周手动导出一次数据（后期可升级 Pro 获得备份）。
- **Vercel Hobby 限制**：无团队协作、构建并发有限——个人项目足够。
- **SEO 迁移**：域名生效后立即提交 sitemap 到 Google Search Console；旧 `vercel.app` 域名保留 301 跳转或直接停用。
- **图片规范**：饮品图建议统一 1:1 正方形、WebP 格式，上传时后台做尺寸提示。

---

## 11. 待确认项

1. 菜单是否需要展示**价格**？（现页面没有价格，方案默认加入 `price` 字段）
2. 老板账号用哪个邮箱？（M3 时在 Supabase 创建）
3. 是否需要「联系我们」表单或 WhatsApp 链接？（现页面只有 Instagram）
